const chai = require('chai');
const chaiHttp = require('chai-http');
const app = require('../app');
const expect = chai.expect;

chai.use(chaiHttp);

describe('User Profile API', () => {
    it('return JWT token when user logs in', (done) => {

    });

    it('create user profile after authentication', (done) => {

    });

    it('should return a list of user profiles after pagination and sorting', (done) => {

    });

    it('should update a user profile after authentication and autherization', (done) => {

    });

    it('should delete a user profile after authenticatio and autherization' (done) => {

    });

});
